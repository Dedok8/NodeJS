npm init -y

npm install express mongoose ejs zod dotenv express-session nodemon cookie-parser connect-flash passport morgan bcrypt

"type": "module"

  "scripts": {
    "start": "node app.mjs"
  },

mkdir controllers,db,models,services,routes,middlewares,validation,config,public,views
mkdir public/javascripts,public/stylesheets

node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"